{{# custommenu }}
    <div class="ucsf_custom_menu row p-t-1">
        <div class="col-12">
            <div class="card">
                <div class="card-block">
                      <ul class="navbar-nav navbar-expand d-none d-md-flex">
                        <!-- categorylabel -->
                        {{{ categorylabel }}}
                        <!-- custommenu -->
                        {{{ custommenu }}}
                      </ul>
                    </nav>
                    <nav class="nav navbar-nav d-md-none pull-left">
                        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                            <i class="icon fa fa-bars fa-fw " aria-hidden="true" aria-label=""></i>
                        </button>
                        <div class="collapse navbar-collapse" id="navbarNav">
                            {{# custommenumobile }}
                                <ul class="list-unstyled p-t-1">
                                    {{> theme_ucsf/custom_menu_mobile }}
                                </ul>
                            {{/ custommenumobile }}
                        </div>
                    </nav>
                </div>
            </div>
        </div>
    </div>
{{/ custommenu }}
